function replace () { grep -rl "$1" . | grep -v "\.svn" | while read name; do echo $name; sed -e "s+$1+$2+g" < "$name" > "$name.new"; mv "$name.new" "$name"; done; }
while read old new; do replace "$old" "$new"; done < ../changes.phase1
